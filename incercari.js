// for (let i = 0; i < product.images.length; i += 1) {
//   newImageSection.append(createImg(product.images[i]));
// // }

// let n = products.id;
//       newImageSection.setAttribute('id', 'n');
//       function createId(id) {
//         const n = newImageSection.setAttribute('id', [products.id.innerText]);
//         products.id = id;
//         return n;
//       }

    //   for (let i = 0; i < 100; i += 1) {
    //     const n = newImageSection.setAttribute('id', 'n');
    //   }

      // for (let i = 0; i < product.images.length; i += 1) {
        //   newImageSection.append(createImg(product.images[i]));
        // // }

// function giveAtribute (tag, give , clasa) {
//   let give = document.getElementsByTagName(tag);
//   give.style.classList.add(clasa)
//   return 
// }

// for (let i = 0; i < 100; i += 1) {
    //     const n = newImageSection[i].setAttribute('id', 'n');
    //   }
  //   for ( i = 0; i < products.id ; i++) 
  //   { 
  //     const giveId = document.querySelectorAll('.newImageSection');
  //     giveId[i].setAttribute('id');
  // }
      // newImageSection.setAttribute('id', 'n');
      // function createId(id) {
      //   const n = newImageSection.setAttribute('id', [products.id.innerText]);
      //   products.id = id;
      //   return n;
      // }